version: '3.8'
services:
  iot-backend:
    build: .
    container_name: iot-backend
    ports:
      - "3001:3000"
    restart: always
    environment:
      - NODE_ENV=production
      - HOIRE_ACCOUNT=${HOIRE_ACCOUNT}
      - HOIRE_PASSWORD=${HOIRE_PASSWORD}
      - APP_URL=${APP_URL}
